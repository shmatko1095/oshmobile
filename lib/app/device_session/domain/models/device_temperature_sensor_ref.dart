class DeviceTemperatureSensorRef {
  const DeviceTemperatureSensorRef({
    required this.id,
    required this.name,
    required this.isReference,
  });

  final String id;
  final String name;
  final bool isReference;
}
